# Redis数据库索引（默认为0）
spring:
  redis:
    database: 0
    host: localhost
    port: 6379
    password: redis
    jedis:
      pool:
        max-idle: 8
        max-wait: 1
        min-idle: 0
        timeou: 5000